version: 2.1

executors:
  my-executor:
    working_directory: /tmp/repo

orbs:
  github-cli: circleci/github-cli@2.2.0

jobs:
  trigger_release:
    executor: my-executor
    machine: true
    steps:
      - github-cli/install
      - run:
          name: Login Github
          command: |
            gh auth login
      - run:
          name: Trigger release
          command: |
            gh api repos/${CIRCLE_PROJECT_USERNAME}/${TARGET_REPO}/releases \
              -X POST \
              -f tag_name=${CIRCLE_TAG} \
              -f target_commitish=${CIRCLE_SHA1} \
              -f name=${CIRCLE_TAG} \
              -f body="Release ${CIRCLE_TAG} from ${CIRCLE_PROJECT_REPONAME}" \
              -f draft=false \
              -f prerelease=false

workflows:
  release-flow:
    jobs:
      - trigger_release:
          context: default
          filters:
            tags:
              only: /^v.*/